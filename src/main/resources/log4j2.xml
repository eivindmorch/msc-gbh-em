<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE Configuration>

<Configuration>

    <Appenders>

        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%highlight{%d{HH:mm:ss.SSS} %-5level %logger{36}.%M() @%L - %msg%n}"/>
        </Console>

        <File name="HlaLib" fileName="logs/hla/hlalib-${date:yyyy-MM-dd-HH-mm-ss}.log" append="false">
            <PatternLayout pattern="%highlight{%d{HH:mm:ss.SSS} %-5level %logger{36}.%M() @%L - %msg%n}{FATAL=red blink, ERROR=red, WARN=yellow bold, INFO=black, DEBUG=green bold, TRACE=blue}"/>
        </File>

        <File name="Training" filename="logs/training/training-${date:yyyy-MM-dd-HH-mm-ss}.log" append="false">
            <PatternLayout pattern="%highlight{%d{HH:mm:ss.SSS} %-5level %logger{36}.%M() @%L - %msg%n}{FATAL=red blink, ERROR=red, WARN=yellow bold, INFO=black, DEBUG=green bold, TRACE=blue}"/>
        </File>

        <File name="DataLogging" fileName="logs/datalogging/dataLogging-${date:yyyy-MM-dd-HH-mm-ss}.log" append="false">
            <PatternLayout pattern="%highlight{%d{HH:mm:ss.SSS} %-5level %logger{36}.%M() @%L - %msg%n}{FATAL=red blink, ERROR=red, WARN=yellow bold, INFO=black, DEBUG=green bold, TRACE=blue}"/>
        </File>

    </Appenders>


    <Loggers>

        <Logger name="no.ffi.hlalib" level="debug" additivity="false">
            <AppenderRef ref="HlaLib"/>
        </Logger>

        <Logger name="datalogging" level="debug" additivity="false">
            <AppenderRef ref="DataLogging"/>
            <AppenderRef ref="Console"/>
        </Logger>

        <Logger name="training" level="debug" additivity="false">
            <AppenderRef ref="Training"/>
            <AppenderRef ref="Console"/>
        </Logger>

        <Root level="error">
            <appender-ref ref="Console"/>
        </Root>

    </Loggers>

</Configuration>